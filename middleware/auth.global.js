export default defineNuxtRouteMiddleware((to,from) => {

    // const isAuth = useCookie('access_token');
    // if (isAuth.value && to.path === '/auth') {
    //     return navigateTo('/');
    // }
    // if (!isAuth.value && to.path !== '/auth' ) {
    //     return navigateTo('/auth');
    // }


})
